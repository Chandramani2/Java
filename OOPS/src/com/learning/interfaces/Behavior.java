package com.learning.interfaces;
/* Concept: Abstraction. An interface defines what an object can do, without defining how it does it. It hides the implementation details.
*/
public interface Behavior {
    // Abstract method: doesn't have a body
    void eat();

    // Abstract method
    void sleep();
}